<% if(typeof errors!='undefined' ) { %>
    <% errors.forEach(function(error){ %>
        <p class="text_ct">
            <%= error.msg %>
        </p>
        <% }) %>
            <% } %>

                <% if(success_msg!='' ) { %>
                    <p class="text_ct">
                        <%=success_msg %>
                    </p>
                    <% } %>

                        <% if(error_msg !='' ) { %>
                            <p class="text_ct">
                                <%= error_msg %>
                            </p>
                            <% } %>
                                <% if(error !='' ) { %>
                                    <p class="text_ct">
                                        <%= error %>
                                    </p>
                                    <% } %>