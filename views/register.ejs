<h1>Register</h1>
<hr>
<p class="text_l">회원가입을 완료하여 언제든 로그인하세요.</p>

<%- include ('./partials/messages') %>
    <form action="/users/register" method="POST" class="Field1">
        <div>
            <label for="name" class="text_Field1">Name</label>
            <input type="name" name="name" placeholder="사용자명 입력"
                value="<%= typeof name != 'undefined' ? name : '' %>" />

        </div>
        <div>
            <label for="email" class="text_Field1">Email</label>
            <input type="email" name="email" placeholder="이메일 주소 입력"
                value="<%= typeof email != 'undefined' ? email : '' %>" />
        </div>
        <div>
            <label for="password" class="text_Field1">Password</label>
            <input type="password" name="password" placeholder="비밀번호 입력"
                value="<%= typeof password != 'undefined' ? password : '' %>" />
        </div>
        <div>
            <label for="password2" class="text_Field1">Confirm Password</label>
            <input type="password" name="password2" placeholder="비밀번호 확인"
                value="<%= typeof password2 != 'undefined' ? password2 : '' %>" />
        </div>
        <button type="submit" class="btn_login btn btn-primary btn-block">회원가입</button>
    </form>
    <div class="Field2">
        <div class="text_s">이미 계정이 있으신가요?</div>
        <button type="button" onclick="location.href='/users/login'">로그인</button>
    </div>

    <button type="button" class="btn_login" onclick="location.href='http://localhost:3000'">처음 페이지로</button>